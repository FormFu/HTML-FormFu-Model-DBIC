---
auto_fieldset: 1

elements:
  - type: Hidden
    name: id
  
  - type: Text
    name: text_col
  
  - type: ComboBox
    name: type_id
    # set this, so options_from_model() populates the menu,
    # so we can test the correct default is set
    model_config:
        resultset: Type

  - type: ComboBox
    name: type2_id
  
  - type: Submit
    name: submit
